# 用户指南

启动后直接输入弹药名、简称、口径或别名，选择护甲预设，拖动当前耐久即可看到解析结果。
蒙特卡洛按钮适合连续射击；任务在后台线程运行，可继续操作界面，新任务会取消旧任务。

快捷键：`Ctrl+K` 搜索、`Ctrl+1/2/3` 聚焦弹药/护甲/结果、`Ctrl+D` 收藏、
`Ctrl+S` 保存预设、`Ctrl+M` 切换实验室、`Ctrl+T` 置顶、`Esc` 清空搜索。

