[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "tarkov-sim-core"
version = "2.1.1"
description = "UI, database and network independent Tarkov simulation core"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
